先定義一個堆疊，然後定義top=0，當堆疊push數字進去時，top跟著+1

在定義一個top()函數，當執行時，將堆疊中最後一個放進去的數字輸出出來，這樣就完成偷看堆疊頂端數字，而不去改變了。

接下來定義peep(i)，假設堆疊X裡有1.5.6.8.9，5個數字，1最先放，然後是5.6.8.9接續，而我們設定的top會跟著設定這些數跟著+1，因此top最後會=4
當執行peep(i)，假設使用者想看堆疊中的第3個數字，輸入3後，則i=i-1，得到peep(2)，藉由讓top-2，來輸出6這個數字。

empty，清空堆疊，則用pop依序將堆疊內的數字推出，先設定empty，執行時，開始pop數字，每pop一個數字，top-1，當top=0時停止。

isEmpty，檢查堆疊是否清空，相對簡單，當執行時，看top是否=0，若=0則傳回true，若否，則傳回false。
